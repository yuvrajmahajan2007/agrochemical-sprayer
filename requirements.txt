# AI-Driven Precision Agrochemical Sprayer - Python dependencies
# Python 3.10 - 3.11 recommended.
# TensorFlow is CPU-only supported on this project; GPU builds work too.

tensorflow>=2.12.0   # models: Plant-validator (MobileNetV2), leaf-health U-Net segmenter
numpy>=1.24.0
Pillow>=10.0.0       # image loading / saving / overlay rendering
Flask>=2.3.0         # backend web server
Flask-CORS>=4.0.0    # allow mobile phone cross-origin requests
matplotlib>=3.7.0    # evaluation reports / confusion matrices
scikit-learn>=1.3.0  # train/val/test splits, metrics
requests>=2.31.0     # backend / dataset helpers
kagglehub>=0.3.0     # optional: convenience downloader for the public PlantVillage dataset
